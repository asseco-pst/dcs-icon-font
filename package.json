{
  "name": "dcs-icon-font",
  "version": "1.0.0",
  "description": "A font generator for generating both font and CSS for SVG files",
  "main": "app/index.js",
  "scripts": {
    "build": "babel src -d app",
    "watch": "watch \"npm run build\" src && node app/index.js"
  },
  "author": "Orêncio Gonçalves",
  "license": "ISC",
  "dependencies": {
    "glob": "^7.0.3",
    "webfonts-generator": "^0.3.5"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "watch": "^0.18.0"
  }
}
