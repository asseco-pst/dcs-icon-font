{
  "name": "dcs-icon-font",
  "version": "1.0.0",
  "description": "A font generator for generating both font and CSS for SVG files",
  "main": "app/index.js",
  "scripts": {
    "start": "node app/index.js",
    "build": "SET NODE_ENV=dev&& npm start",
    "build-prod": "SET NODE_ENV=prod && npm start",
    "compile": "babel src -d app",
    "watch": "watch \"npm run compile\" src"
  },
  "author": "Orêncio Gonçalves",
  "license": "ISC",
  "dependencies": {
    "glob": "^7.0.3",
    "webfonts-generator": "^0.3.5"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "watch": "^0.18.0"
  }
}
